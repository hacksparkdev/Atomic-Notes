Tags: [[Bash]] [[Scripting]] #UsefulCommands 

### Directory Count 
#DirectoryCount
```Bash
# This will give a list of folders in a directory 
Ls | wc -l
```


### Add something to a file
#EchoIntoFile
```Bash
echo 'hash' > hash.txt
```

